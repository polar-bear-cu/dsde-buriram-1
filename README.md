# dsde-buriram-1

## Project Structure

```
dsde-buriram-1/
├── data/
│   ├── district/         # ข้อมูลการเลือกตั้ง สส. แบบแบ่งเขต
│   │   ├── advance/      # ข้อมูลการเลือกตั้งล่วงหน้า
│   │   └── ontime/       # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
│   ├── partylist/        # ข้อมูลการเลือกตั้ง สส. แบบบัญชีรายชื่อ
│   │   ├── advance/      # ข้อมูลการเลือกตั้งล่วงหน้า
│   │   └── ontime/       # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
│   └── referendum/       # ข้อมูลการลงประชามติ
│        ├── inside/      # ข้อมูลการลงคะแนนในเขต
│        └── outside/     # ข้อมูลการลงคะแนนนอกเขต
├── flatten_result/     # ข้อมูลที่ผ่านการ process แล้ว
├── ocr_output/         # ผลลัพธ์ที่ได้จากการทำ ocr
│   ├── district/       # ข้อมูลการเลือกตั้ง สส. แบบแบ่งเขต
│   │   ├── advance/    # ข้อมูลการเลือกตั้งล่วงหน้า
│   │   └── ontime/     # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
│   ├── partylist/      # ข้อมูลการเลือกตั้ง สส. แบบบัญชีรายชื่อ
│   │   ├── advance/    # ข้อมูลการเลือกตั้งล่วงหน้า
│   │   └── ontime/     # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
│   └── referendum/     # ข้อมูลการลงประชามติ
│       ├── inside/     # ข้อมูลการลงคะแนนในเขต
│       └── outside/    # ข้อมูลการลงคะแนนนอกเขต
├── ocr_output_clean/         # ไฟล์จาก ocr ที่แปลงแล้ว
│   ├── district/             # ข้อมูลการเลือกตั้ง สส. แบบแบ่งเขต
│   │   ├── advance/          # ข้อมูลการเลือกตั้งล่วงหน้า
│   │   └── ontime/           # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
│   ├── partylist/            # ข้อมูลการเลือกตั้ง สส. แบบบัญชีรายชื่อ
│   │   ├── advance/          # ข้อมูลการเลือกตั้งล่วงหน้า
│   │   └── ontime/           # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
│   └── referendum/           # ข้อมูลการลงประชามติ
│       ├── inside/           # ข้อมูลการลงคะแนนในเขต
│       └── outside/          # ข้อมูลการลงคะแนนนอกเขต
├── 01_ocr_district.ipynb           # ไฟล์สำหรับการทำ ocr กลุ่มสส. แบบแบ่งเขต
├── 01_ocr_partylist.ipynb          # ไฟล์สำหรับการทำ ocr กลุ่มสส. แบบบัญชีรายชื่อ
├── 01_ocr_referendum.ipynb         # ไฟล์สำหรับการทำ ocr กลุ่มการลงประชามติ
├── 02_process.ipynb                # ไฟล์สำหรับการวิเคราะห์ข้อมูล
├── 02_validation_issues.csv        # ไฟล์สำหรับตรวจสอบความไม่สอดคล้องของข้อมูล
├── 02_validation_issues_clean.csv  # ไฟล์สำหรับแสดงความไม่สอดคล้องของข้อมูลหลังจากการแก้ไขข้อมูล
├── utils.py                        # ไฟล์รวม function ต่างๆ
```

Note: Latest Update - 02/05/2026 06:00

## Stack

- **Typhoon OCR**

## OCR Result Format

- District (เขต)

```
{
  "name": "...",
  "summary": {
    "จำนวนบัตรทั้งหมด": 0,
    "บัตรดี": 0,
    "บัตรเสีย": 0,
    "ไม่เลือกผู้ใด": 0
  },
  "data": [
    {
        "ชื่อผู้สมัคร": "",
        "พรรค": "",
        "คะแนน": 0
    },
    ...
  ]
}
```

- Partylist (บัญชีรายชื่อ)

```
{
  "name": "...",
  "summary": {
    "จำนวนบัตรทั้งหมด": ...,
    "บัตรดี": ...,
    "บัตรเสีย": ...,
    "ไม่เลือกผู้ใด": ...
  },
  "data": [
    {
      "พรรค": "",
      "คะแนน": ...
    },
    ...
  ]
}
```

- Referendum (ประชามติ)

```
{
  "name": "...",
  "summary": {
    "ผู้มีสิทธิ": ...,
    "ผู้มาใช้สิทธิ": ...,
    "บัตรเสีย": ...,
  },
  "data": [
    {
      "รายการ": "เห็นชอบ",
      "คะแนน": ...
    },
    {
      "รายการ": "ไม่เห็นชอบ",
      "คะแนน": ...
    },
    {
      "รายการ": "ไม่แสดงความคิดเห็น",
      "คะแนน": ...
    }
  ]
}
```
