# dsde-buriram-1

## Project Structure

```
dsde-buriram-1/
├── ocr_output/         # ผลลัพธ์ที่ได้จากการทำ ocr
│   ├── district/       # ข้อมูลการเลือกตั้ง สส. แบบแบ่งเขต
│   │   ├── advance/    # ข้อมูลการเลือกตั้งล่วงหน้า
│   │   └── ontime/     # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
│   ├── partylist/      # ข้อมูลการเลือกตั้ง สส. แบบบัญชีรายชื่อ
│   │   ├── advance/    # ข้อมูลการเลือกตั้งล่วงหน้า
│   │   └── ontime/     # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
│   └── referendum/     # ข้อมูลการลงประชามติ
│       ├── inside/     # ข้อมูลการลงคะแนนในเขต
│       └── outside/    # ข้อมูลการลงคะแนนนอกเขต
├── ocr_output_clean/         # ไฟล์จาก ocr ที่แปลงแล้ว
│   ├── district/             # ข้อมูลการเลือกตั้ง สส. แบบแบ่งเขต
│   │   ├── advance/          # ข้อมูลการเลือกตั้งล่วงหน้า
│   │   └── ontime/           # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
│   ├── partylist/            # ข้อมูลการเลือกตั้ง สส. แบบบัญชีรายชื่อ
│   │   ├── advance/          # ข้อมูลการเลือกตั้งล่วงหน้า
│   │   └── ontime/           # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
│   └── referendum/           # ข้อมูลการลงประชามติ
│       ├── inside/           # ข้อมูลการลงคะแนนในเขต
│       └── outside/          # ข้อมูลการลงคะแนนนอกเขต
├── ocr/                        # ไฟล์สำหรับการทำ ocr
│   ├── district.ipynb          # กลุ่มสส. แบบแบ่งเขต
│   ├── partylist.ipynb         # กลุ่มสส. แบบบัญชีรายชื่อ
│   └── referendum.ipynb        # กลุ่มการลงประชามติ
├── main.ipynb                  # ไฟล์สำหรับการวิเคราะห์ข้อมูล
└── data/
    ├── district/       # ข้อมูลการเลือกตั้ง สส. แบบแบ่งเขต
    │   ├── advance/    # ข้อมูลการเลือกตั้งล่วงหน้า
    │   └── ontime/     # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
    ├── partylist/      # ข้อมูลการเลือกตั้ง สส. แบบบัญชีรายชื่อ
    │   ├── advance/    # ข้อมูลการเลือกตั้งล่วงหน้า
    │   └── ontime/     # ข้อมูลการเลือกตั้งในวันเลือกตั้งจริง
    └── referendum/     # ข้อมูลการลงประชามติ
        ├── inside/     # ข้อมูลการลงคะแนนในเขต
        └── outside/    # ข้อมูลการลงคะแนนนอกเขต
```

Note: Latest Update - 27/04/2026 17:56

## Stack

- **Typhoon OCR**

## OCR Result Format

- District (เขต)

```
{
  "name": "...",
  "summary": {
    "จำนวนบัตรทั้งหมด": 0,
    "บัตรดี": 0,
    "บัตรเสีย": 0,
    "ไม่เลือกผู้ใด": 0
  },
  "data": [
    {
        "ชื่อผู้สมัคร": "",
        "พรรค": "",
        "คะแนน": 0
    },
    ...
  ]
}
```

- Partylist (บัญชีรายชื่อ)

```
{
  "name": "...",
  "summary": {
    "จำนวนบัตรทั้งหมด": ...,
    "บัตรดี": ...,
    "บัตรเสีย": ...,
    "ไม่เลือกผู้ใด": ...
  },
  "data": [
    {
      "พรรค": "",
      "คะแนน": ...
    },
    ...
  ]
}
```

- Referendum (ประชามติ)

```
{
  "name": "...",
  "summary": {
    "ผู้มีสิทธิ": ...,
    "มาใช้สิทธิ": ...,
  },
  "data": [
    {
      "รายการ": "เห็นชอบ",
      "คะแนน": ...
    },
    {
      "รายการ": "ไม่เห็นชอบ",
      "คะแนน": ...
    },
    {
      "รายการ": "ไม่แสดงความคิดเห็น",
      "คะแนน": ...
    }
  ]
}
```
